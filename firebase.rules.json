{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$code": {
        ".read": true,
        ".write": true,
        ".validate": "$code.length === 4 && newData.hasChild('meta') || data.exists()"
      }
    }
  }
}
